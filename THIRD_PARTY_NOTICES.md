# Third-party notices

- **CMU Pronouncing Dictionary:** bundled at `public/cmudict.dict` for compatibility with the frozen contest rules. Its notice is retained in `public/CMUDICT-LICENSE.txt`.
- **Shadcn UI components:** the bundled `components/ui/` files retain the upstream notice in `vendor/shadcn-ui.LICENSE.md`.
- **Shadcn Tailwind styles:** bundled at `vendor/shadcn-tailwind-4.13.0.css`, with the corresponding license in `vendor/shadcn-tailwind-4.13.0.LICENSE.md`.
- **PACT vault compatibility fixture:** `tests/fixtures/pact-v1-source.txt` retains its source URL and revision. It contains code for generating test identities, not a participant vault.
- **Runtime and development dependencies:** their versions are recorded in `pnpm-lock.yaml`; each dependency retains its own license in its distribution.

The repository's MIT license applies to project code and does not replace the licenses above. FLOP Labs and Technocore names identify the upstream project and contest, not an endorsement.
