import {sql} from "drizzle-orm";
import {sqliteTable,text,integer,index} from "drizzle-orm/sqlite-core";
export const records=sqliteTable("sonnet_records",{id:text("id").primaryKey(),room:text("room").notNull(),generation:integer("generation").notNull(),seq:integer("seq").notNull(),body:text("body").notNull(),ts:text("ts").notNull()},t=>[index("idx_sonnet_records_room_generation").on(t.room,t.generation),index("idx_sonnet_records_room_seq").on(t.room,t.generation,t.seq),index("idx_sonnet_records_vote_receipts").on(t.room,sql`json_extract(${t.body}, '$.from')`,t.id).where(sql`json_extract(${t.body}, '$.payload.type')='sonnet.receipt.v1' AND json_extract(${t.body}, '$.payload.status')='accepted'`),index("idx_sonnet_records_sender").on(sql`json_extract(${t.body}, '$.from')`),index("idx_sonnet_records_receipt_sender").on(sql`json_extract(${t.body}, '$.payload.sender_did')`),index("idx_sonnet_records_request").on(t.room,t.generation,sql`json_extract(${t.body}, '$.from')`,sql`json_extract(${t.body}, '$.payload.request_id')`)]);
export const cursors=sqliteTable("sonnet_cursors",{room:text("room").primaryKey(),generation:integer("generation").notNull(),seq:integer("seq").notNull(),first:integer("first_seq").notNull(),updatedAt:text("updated_at").notNull()});
export const cache=sqliteTable("sonnet_cache",{key:text("key").primaryKey(),value:text("value").notNull(),expires:integer("expires").notNull()});
